<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>

<p>
  <div style="width:400px; height:auto">
    <%= image_tag(@team.crest, style:'width:100%') if @team.crest.attached? %>
  </div>
</p>

<%= link_to 'Edit', edit_team_path(@team) %> |
<%= link_to 'Back', teams_path %>
